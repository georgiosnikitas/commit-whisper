# Deferred Work

Tracked items deferred from reviews and other workflows. Each entry names its source and date.

## Deferred from: code review of 1-1-project-scaffold-and-toolchain (2026-06-13)

- **No `bin`/shebang for the CLI** — `package.json` has no `bin` field and `tsup.config.ts` adds no `#!/usr/bin/env node` banner, so `dist/index.js` can't yet be invoked as a command. Out of scope for the scaffold; CLI entry wiring belongs to later Epic 1 stories (notably 1.8, end-to-end strict single-shot run). Revisit when the `cli/` shell is implemented.
- **`shared/ui.ts` will collide with the `no-console` rule** — the ESLint config applies `no-console: error` to `src/shared/**`, but the architecture designates `src/shared/ui.ts` as the single human-output writer to stderr. When `ui.ts` lands, implement it with `process.stderr.write` (idiomatic, avoids the rule) or add a narrowly-scoped override for that one file. Address in the story that introduces `ui.ts`.
- **Root config files escape `tsc` typecheck** — `tsconfig.json` `include: ["src"]` excludes `tsup.config.ts`, `vitest.config.ts`, and `eslint.config.js`, so type errors in build/test/lint config are never caught by `tsc --noEmit`. Minor; consider a dedicated config-typecheck pass (e.g. a second tsconfig) in a later tooling/CI story.

## Deferred from: code review of 1-2-two-phase-configuration-resolver-and-frozen-runconfig (2026-06-13)

- **Env value validation/normalization bundle** [src/config/env.ts] — decimal + safe-integer `maxCommits` (reject hex `0x10`, exponent `1e3`, and `> Number.MAX_SAFE_INTEGER` precision loss); case-insensitive enum parsing for `aiMode`/`provider`/`branch`/`format` (today `COMMIT_SAGE_AI_MODE=OFF` is silently ignored → can leave AI on in interactive `auto`); IANA `timezone` validation; ISO date validation + `startDate ≤ endDate`; `llmBaseUrl` URL validation (SSRF-relevant once `narrate` issues requests in Story 1.6); `outputFormats` dedup (`html,html` → double render), reject-invalid, and enforce ≥1. Deferred because the Story 1.2 Dev Notes explicitly scope value validation to the Zod schema story; 1.2's AC bar is pure merge + provenance + capability gate + typed gap-error, and `env.ts` is documented to coerce leniently and omit invalid values.
- **Treat JSON `null` as "not supplied"** [src/config/resolver.ts, src/config/gaps.ts] — `mergeLayers` uses `value !== undefined` and `finalizeRunConfig` checks `=== undefined`, so a `null` from a real config file would override a lower layer and/or pass the required-field check. Not reachable in 1.2 (the `PartialRunConfig` types forbid `null` and `configFile` is injected `{}`); address when Epic 6 wires the `~/.commit-sage` JSON reader.
- **Broaden + normalize CI detection** [src/config/capability.ts] — `detectCI` consults only `env.CI` and matches `"false"`/`"0"` case-sensitively, so `CI=False`/`CI=FALSE`/`CI=" "` are treated as CI (→ headless, the safe direction) while a deliberate `CI=False` is not honored as "not CI." Consider `CONTINUOUS_INTEGRATION` + provider-specific markers (or `ci-info`) and trim+lowercase the value. Deferred: current behavior fails closed; Task 4 listed broader markers only as a nice-to-have.
- **Single-source-of-truth for the `provider`/`format`/`aiMode` enums** [src/config/env.ts, src/config/run-config.ts] — the runtime `Set<string>` validators in `env.ts` are decoupled from the union types in `run-config.ts`, so adding/renaming a union member without updating the set compiles cleanly but mis-parses. Derive both the union and the runtime set from one `as const satisfies readonly Provider[]` array. Fold into the Zod schema story (the schema becomes the canonical enum source).
- **Enforce `entitlement.commitCap` against `maxCommits`** [src/config/resolve-run-config.ts] — the resolver carries `entitlement` but never clamps `maxCommits` to the tier cap. By design: the Free 100-commit cap mechanics (date-then-cap ordering, truncation notice) are Epic 2.
- **Replace the `as RunConfig` cast with an explicit mandatory-field invariant** [src/config/gaps.ts] — `FIELD_SPECS` marks the defaulted-mandatory fields (`branch`, `timezone`, `noMerges`, `outputFormats`, `aiMode`) `optional`, so `finalizeRunConfig` does not validate them and the `... as RunConfig` cast would emit a frozen object with `undefined` "required" fields if ever called without the defaults layer merged in. Safe today (the sole caller always merges `buildDefaults`, and the precondition is documented in the file header + exercised only by tests that build full partials); harden later by asserting the mandatory fields are present instead of casting.

## Deferred from: code review of 1-3-error-model-exit-codes-and-stream-discipline (2026-06-13)

- **Error `cause` chaining** [src/shared/errors.ts] — the `CommitSageError` constructors accept only a message, so wrapping a lower-level failure (git spawn, fetch, fs, parse) discards the originating error. Add an optional `{ cause }` to the base constructor and thread it through the stage subclasses. Deferred so the API is driven by the first real wrapping call site — Story 1.4 (`RetrieveError` over a git/spawn failure) — rather than added speculatively; it is a cheap ES2022 addition but touches every subclass signature, so do it once with a concrete consumer.
- **`ui` stream robustness** [src/shared/ui.ts] — `writeLine` ignores `stream.write`'s backpressure return (`false` ⇒ should await `drain`), does not guard a synchronous throw, and adds no async `error`/`EPIPE` listener, so piping human output to a consumer that closes early (`commit-sage ... | head`) or a destroyed stream can crash the CLI mid-output. Belongs to the `ui` expansion + the Story 1.8 process shell where stream lifecycle and the top-level `process.on` handlers live; the minimal injected-stream writer is correct for the 1.3 slice.
- **Tighten error typing** [src/shared/errors.ts, src/cli/exit-codes.ts] — `CommitSageError.code` is an unconstrained `string` (typos like `"RETRIVE"` compile) and `exitCodeForError` returns a bare `number` rather than `ExitCode`, with no range check that a thrown error's `exitCode ∈ 1..8`. A type-strictness pass could make `code` a union and narrow the return. Deferred: the real drift risk (literal-vs-enum) is already covered by the `exit-codes.test.ts` consistency block, and the `: number` signature is currently spec-exact (the `shared/` subclasses cannot import the `cli/` `ExitCode` type).
- **Sanitize control / ANSI characters in stderr output** [src/shared/ui.ts, src/shared/errors.ts] — neither the error constructors nor `ui` strip terminal escape / control characters, so any message that ever interpolates external content (e.g. a repo path or branch name) could inject ANSI sequences into the user's terminal. Low severity today (messages are built from our own templates), but worth a single sanitization point at the `ui` boundary when color/formatting lands in Epic 6.

## Deferred from: code review of 1-4-local-repository-retrieval (2026-06-13)

- **Delimiter collision in the git-log parser** [src/retrieve/git-log.ts] — a commit message body (`%B`) that contains a literal RS (`\x1e`) or US (`\x1f`) byte would forge a record boundary or truncate fields (the parser splits naively on those bytes). Harden by realigning fields from the head and splitting RS only at a real boundary (RS immediately followed by a 40-hex sha), or switch the format to NUL (`%x00`) / `git log -z` framing. Tighten the `parts.length < 9` guard to `< 10` (the format always yields ≥10 fields) at the same time. Deferred: RS/US are non-printing controls that effectively never appear in real commit text, so this is a hardening pass, not a live bug.
- **No subprocess timeout** [src/retrieve/git.ts] — `execFileGitRunner` sets no `timeout`, so a git that blocks on a credential prompt, an fsmonitor daemon, or a wedged process hangs the CLI indefinitely with no recovery. Add an `execFile` `timeout` + `killSignal` and map a timeout to a `RetrieveError`. Deferred to pair with the Story 1.8 process shell and the Epic 5 remote-clone network-failure handling, where timeout/abort policy is load-bearing.
- **`spawn`-streaming for very large histories** [src/retrieve/git.ts, src/retrieve/git-log.ts] — the 256 MB `maxBuffer` both hard-caps huge monorepos (git is killed and all output discarded) and roughly doubles peak memory because `parseGitLog` does `stdout.split(RS)` on the whole materialized string. Stream the log line-by-line (or record-by-record) instead. A perf refinement already noted in the code comment; not needed for the walking skeleton.
- **Surface git's stderr / preserve failure specificity** [src/retrieve/git.ts, src/retrieve/local.ts] — the runner returns only stdout and the adapter flattens every read failure into one generic message, so a `maxBuffer` overflow, a permission denial, and a genuine repo error are indistinguishable to the user. The underlying error is already preserved as `cause`; richer stderr surfacing belongs with the `ui`/verbose-logging work (Epic 6) and the 1.8 shell.
- **Harden rename-path resolution** [src/retrieve/git-log.ts] — `resolveNumstatPath`'s greedy `^(.*)\{(.*) => (.*)\}(.*)$` regex plus the blanket `//`→`/` collapse can misfire on paths that legitimately contain braces, doubled slashes, or a literal ` => ` substring. Gate rename resolution on git's actual rename status (e.g. parse with `--raw`/status flags) rather than substring-matching the numstat path. Deferred: the current code handles the common brace/plain `old => new` forms (already documented as a simplification); exotic paths are rare and non-blocking.

## Deferred from: code review of 1-5-metrics-engine-framework-determinism-harness-and-group-a (2026-06-13)

- **Strict ISO-8601-with-offset format validation** [src/analyze/model.ts] — beyond the non-finite `Date.parse` guard added in review (Patch 3), assert each commit timestamp carries an explicit `Z`/`±hh:mm` offset, so a future non-git input source cannot smuggle in an offset-less string that `Date.parse` would interpret as host-local time (a cross-machine determinism leak). Deferred: git's `%cI`/`%aI` always emit offsets, and full format validation belongs with the config/input Zod validation story.
- **`Intl` ICU / tzdata determinism boundary** [src/analyze/time.ts] — tz-aware bucket labels for historical/future instants depend on the runtime's bundled ICU/CLDR + tz database, so "identical input ⇒ identical JSON" holds only within one exact runtime. Document this boundary and consider pinning ICU (`full-icu`) or a vendored tz table if cross-runtime reproducibility is ever required. Deferred: inherent cost of dependency-free tz handling; Node 22 ships full ICU; the CI trend-diff target (same runtime) is unaffected.
- **Invalid/unknown timezone + small-ICU fallback** [src/analyze/time.ts] — `formatterFor` throws `RangeError` on an unrecognized IANA zone or a small-ICU Node build, which the engine's catch turns into `not_available` for the three tz-bucketed metrics. Add a `try/catch` fallback to UTC (with a surfaced note) or validate `timezone` upstream. Deferred: timezone validation belongs with the config Zod story; behavior degrades gracefully (no crash) today.
- **Unicode NFC/NFD identity normalization** [src/analyze/identity.ts] — names/emails that differ only by Unicode normal form fragment into separate canonical identities. Add `.normalize("NFC")` to the identity-key derivation. Deferred: rare; fold into an identity/mailmap hardening pass when real `.mailmap` reading lands (Story 1.8).
- **`parseMailmap` hardening** [src/analyze/identity.ts] — handle a `#` appearing inside a quoted segment (currently truncates the line), lines with ≥3 `<>` segments, and an empty canonical name. Deferred: the parser handles the documented common `.mailmap` forms and skips malformed lines safely; exotic forms are rare.
- **Author grouping key (email vs email+name)** [src/analyze/model.ts] — `summarizeAuthors` keys by `email\x00name`, so the same author committing under differing names (without a `.mailmap`) fragments into multiple `AuthorSummary` entries. Consider keying by canonical email alone (the documented identity key). Deferred: this is an author-identity **semantics** decision (git's own default groups by name+email; mailmap is the intended canonicalizer), not a clear-cut bug — revisit with Group B (Contribution & Ownership), which owns author analytics, in Epic 2.
- **`stats` non-finite guards + percentile clamping** [src/analyze/stats.ts] — return `null` on `NaN`/`Infinity` input and clamp `p` to `0..100` in `percentile`. Deferred: once `buildModel` validates timestamps (review Patch 3), all current metric inputs are finite, so this is belt-and-suspenders; add it alongside the next stats consumer (Group E churn/hotspots, Epic 2).
- **Calendar-day vs absolute-duration + weekday conventions** [src/analyze/groups/a-cadence.ts, src/analyze/time.ts] — `gapDays`/`lifespanDays`/`ageDays` divide elapsed seconds by a flat `86400` (not DST/calendar-day aware), and the report mixes a Monday-based ISO-week bucket with a Sunday=0 `byWeekday` histogram. Document the conventions (and decide whether durations should be calendar-day-aware). Deferred: conventional and fully deterministic today; a presentation/clarity refinement, not a correctness defect.




